<Window x:Class="Market_viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Market_viewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="0 0 0 20">
        <ListBox x:Name="listOfTic" Margin="10" HorizontalAlignment="Left">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding id}" FontWeight="Bold"/>
                        <TextBlock Text=" , " />
                        <TextBlock Text="{Binding name}"/>
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListView x:Name="listOfWallet" Margin="10" HorizontalAlignment="Right">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding id}" FontWeight="Bold"/>
                        <TextBlock Text=", " />
                        <TextBlock Text="{Binding amount}" />
                        <TextBlock Text=", " />
                        <TextBlock Text="{Binding tickerId}" />
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>